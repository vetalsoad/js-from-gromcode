for (let index = 1; index <= 5; index++) {
    console.log('\n=> ' + index + ' <= \n\n');

    for (let numbers = 1; numbers <= 10; numbers++) {
        console.log(index + ' x ' + numbers + ' = ' +
            numbers * index);
    }
}